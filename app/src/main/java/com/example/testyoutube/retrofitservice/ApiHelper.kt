package com.example.testgiphy.retrofitservice

class ApiHelper (private val apiService: ApiService) {
    //suspend fun getGiphy(offset: Int): GiphyResponse = apiService.getGiphy(PAGING_SIZE, offset, "g")
}